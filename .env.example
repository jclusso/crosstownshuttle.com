# Copy to .env for local work. Never commit .env.
# On Netlify this lives in Site configuration > Environment variables.

# The single password the band types at /band-admin.
ADMIN_PASSWORD=pick-a-long-passphrase
